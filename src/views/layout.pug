include language/layout-idiomas

doctype

html 
    head 
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width,initial-scale=1")
        link(rel="shortcut icon" href="/public/images/milton-logo.png")
        link(rel="preconnect" href="https://fonts.googleapis.com")
        link(rel="preconnect" href="https://fonts.gstatic.com", crossorigin)
        link(href="https://fonts.googleapis.com/css2?family=Rubik&display=swap" rel="stylesheet")
        link(rel="stylesheet" href="/bscss/bootstrap.min.css")
        link(rel="stylesheet" href="/public/css/layout.css")
        block links
    body
        header 
            nav.navbar.fixed-top.navbar-expand-lg.navbar-light(style="background-color: #515e7b")
                a.navbar-brand(style="margin-left: 16px;" href="#")
                    img(src="/public/images/milton-logo.png" style="width: 40px; border-radius: 10px;" alt="Logo Milton Santos")
                a.navbar-brand.link-image(style="margin-left: 10px; color: white;" href=`/inicio/${idioma}`).

                    Milton Santos

                button.navbar-toggler(type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Alterna navegação")
                    span.navbar-toggler-icon
                .container-fluid.d-flex.justify-content-between
                    .container-fluid.d-flex.justify-content-start
                        .collapse.navbar-collapse#navbarNavDropdown
                            ul.navbar-nav
                                li.nav-item.active
                                    a.nav-link(style="color: white;" href=`/inicio/${idioma}`)
                                        i.fas.fa-home
                                        =` ${linkHome[idioma]}`
                                        span.sr-only (Página atual)
                                li.nav-item
                                    a.nav-link(style="color: white; font-weight: 1700; font-style: italic;" href=`/milton-santos/${idioma}`)
                                        i.fas.fa-book
                                        =` ${linkWhy[idioma]}`
                                li.nav-item
                                    a.nav-link(style="color: white;" href=`/contato/${idioma}`)
                                        i.fas.fa-envelope
                                        =` ${linkContact[idioma]}`
                                li.nav-item.dropdown
                                    a#navbarDropdownMenuLink.nav-link.dropdown-toggle(style="color: white;" href='#' role='button' data-toggle='dropdown' aria-haspopup='true' aria-expanded='false')
                                        i.fas.fa-question-circle
                                        =` ${linkWho[idioma]}`
                                    .dropdown-menu.dropdown-menu-right(aria-labelledby='navbarDropdownMenuLink' style="background-color:#183153;")
                                        a.dropdown-item.text-light(href=`/neabi/${idioma}`)=`${linkNEABI[idioma]}`
                                        a.dropdown-item.text-light(href=`/professores/${idioma}`)=`${linkTCH[idioma]}`
                                        a.dropdown-item.text-light(href=`/estudantes/${idioma}`)=`${linkSTD[idioma]}`
                                        a.dropdown-item.text-light(href=`/malungos/${idioma}`) Malungos
                    .collapse.navbar-collapse#navbarNavDropdown
                        ul.navbar-nav(style="padding: 0 15px 0 15px;")
                            li.nav-item.dropdown
                                a.nav-link.dropdown-toggle#navbarDropdownMenuLink(style="color: white;" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false")
                                    i.fas.fa-language
                                .dropdown-menu.dropdown-menu-right(aria-labelledby="navbarDropdownMenuLink" style="background-color:#183153;")
                                    a.dropdown-item.text-light(style=`background-color: ${back2[idioma]};` href="#" onClick='return reloadPage("br")')
                                        img.image-icon(src="/public/images/brasil.png" alt="Bandeira do Brasil")
                                        =`  ${linkPT[idioma]}`
                                    a.dropdown-item.text-light(style=`background-color: ${back[idioma]};` href="#" onClick='return reloadPage("en")')
                                        img.image-icon(src="/public/images/estados-unidos.png" alt="Bandeira dos EUA")
                                        =`  ${linkEN[idioma]}`
                                    .dropdown-divider
                                    a.dropdown-item.text-light(href="" onClick='return aviso()')
                                        img.image-icon(src="/public/images/alemanha.png" alt="Bandeira da Alemanha")
                                        =` ${linkCS[idioma]}`
                                    a.dropdown-item.text-light(href="" onClick='return aviso()')
                                        img.image-icon(src="/public/images/espanha.png" alt="Bandeira da Espanha")
                                        =` ${linkCS[idioma]}`
                                    a.dropdown-item.text-light(href="" onClick='return aviso()')
                                        img.image-icon(src="/public/images/italia.png" alt="Bandeira da Itália")
                                        =` ${linkCS[idioma]}`
            .container-fluid.sombra-nav
            br
            br
            br

        main 
            block principal 
            br
            br

        footer.rodape
            img.imagemRod(src="/public/images/ifms.png")
            br
            br
            i.far.fa-copyright
            =` ${textCopy[idioma]}`
            br
            br
            | Adriel Carmo
            br
            | Vinícius Batista
            br
            | João Haufes

script(src="/jquery/jquery.slim.min.js")
script(src="/popperjs/popper.min.js")
script(src="/bsjs/bootstrap.min.js")
script(src="https://kit.fontawesome.com/83e2f67088.js" crossorigin="anonymous")

// Função RECARREGAR PÁGINA - IDIOMA
script.

    function reloadPage(idiomaSelec){
        let url = window.location.href;
        url = url.substring(0, (url.length-2)) + idiomaSelec;
        window.location.href = url;
    }

    function aviso() {
        window.alert("Ainda não está pronto amiguinho... EM BREVE!");
    }